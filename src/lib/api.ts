function normalizeBase(value:string){const raw=value.trim().replace(/\/$/,'');if(!raw)return 'https://api.priyasa.com/api/v1';return raw.endsWith('/api/v1')?raw:`${raw}/api/v1`}
const SERVER_BASE=normalizeBase(process.env.PRIYASA_API_BASE_URL||process.env.NEXT_PUBLIC_API_URL||'https://api.priyasa.com');
const BROWSER_BASE='/api/priyasa';
export type ApiResult<T=unknown>={success?:boolean;data?:T;message?:string;errors?:Record<string,string[]>};
function requestId(){return typeof crypto!=='undefined'&&'randomUUID' in crypto?crypto.randomUUID():`${Date.now()}-${Math.random().toString(36).slice(2)}`}
export async function api<T=unknown>(path:string,init:RequestInit={}){const browser=typeof window!=='undefined';const base=browser?BROWSER_BASE:SERVER_BASE;const token=browser?localStorage.getItem('priyasa_admin_token'):null;const headers=new Headers(init.headers);const method=(init.method||'GET').toUpperCase();headers.set('Accept','application/json');if(init.body&&!(init.body instanceof FormData)&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');if(token)headers.set('Authorization',`Bearer ${token}`);if(!headers.has('X-Request-ID'))headers.set('X-Request-ID',requestId());if(['POST','PUT','PATCH','DELETE'].includes(method)&&!headers.has('Idempotency-Key'))headers.set('Idempotency-Key',requestId());const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),30000);let signal=controller.signal;if(init.signal){signal=typeof AbortSignal!=='undefined'&&'any' in AbortSignal?AbortSignal.any([controller.signal,init.signal]):init.signal}try{const res=await fetch(`${base}${path.startsWith('/')?path:`/${path}`}`,{...init,method,headers,cache:'no-store',credentials:'include',signal});if(res.status===204)return {success:true} as ApiResult<T>;const text=await res.text();let body:ApiResult<T>={};try{body=text?JSON.parse(text):{}}catch{body={message:'The API returned a non-JSON response'}}if(res.status===401&&typeof window!=='undefined'){localStorage.removeItem('priyasa_admin_token');localStorage.removeItem('priyasa_admin_user');if(window.location.pathname!=='/login')window.location.assign('/login')}if(res.status===403){throw new Error(body.message||'You are not authorized to perform this action.')}if(!res.ok){const validation=body.errors?Object.values(body.errors).flat().join(' '):'';throw new Error(validation||body.message||`API request failed (${res.status})`)}return body}catch(error){if(error instanceof DOMException&&error.name==='AbortError')throw new Error('Request timed out. Please retry.');throw error}finally{clearTimeout(timeout)}}
export const apiBase=typeof window==='undefined'?SERVER_BASE:BROWSER_BASE;
