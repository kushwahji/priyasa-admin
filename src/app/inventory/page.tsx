'use client';
import {useEffect,useState} from 'react';import {api} from '@/lib/api';import {Card,PageHeader,DataToolbar,Button,Modal,Loading,ErrorState} from '@/components/ui';
export default function Inventory(){const [rows,setRows]=useState<any[]>([]);const [q,setQ]=useState('');const [selected,setSelected]=useState<any>(null);const [qty,setQty]=useState('');const [reason,setReason]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function load(){try{const r=await api<any>('/admin/inventory');const d=r.data;setRows(Array.isArray(d)?d:d?.data||d?.items||[])}catch(e){setError(e instanceof Error?e.message:'Request failed')}}useEffect(()=>{load()},[]);const filtered=rows.filter(v=>JSON.stringify(v).toLowerCase().includes(q.toLowerCase()));async function adjust(){if(!selected)return;setBusy(true);try{await api(`/admin/inventory/${selected.id}/adjust`,{method:'POST',body:JSON.stringify({quantity:Number(qty),reason})});setSelected(null);setQty('');setReason('');load()}catch(e){setError(e instanceof Error?e.message:'Adjustment failed')}finally{setBusy(false)}}return <section className="content"><PageHeader title="Inventory" description="Monitor stock and make controlled adjustments."/><Card><DataToolbar value={q} onChange={setQ} placeholder="Search SKU, variant…"/>{error&&<ErrorState error={error} onRetry={load}/>} {!rows.length&&!error?<Loading/>:filtered.length?<div className="table-wrap"><table className="table"><thead><tr><th>Variant</th><th>SKU</th><th>Quantity</th><th>Reserved</th><th>Available</th><th/></tr></thead><tbody>{filtered.map((v:any,i)=><tr key={String(v.id||i)}><td><b>{v.name||v.product?.name||v.variant_name||'—'}</b></td><td>{v.sku||v.variant?.sku||'—'}</td><td>{v.quantity??v.stock??v.inventory?.quantity??'—'}</td><td>{v.reserved??v.inventory?.reserved??'—'}</td><td>{v.available??v.inventory?.available??'—'}</td><td>{v.id&&<Button onClick={()=>setSelected(v)}>Adjust</Button>}</td></tr>)}</tbody></table></div>:<div className="empty">No inventory records match the search.</div>}</Card>{selected&&<Modal title="Adjust inventory" onClose={()=>setSelected(null)}><form className="form" onSubmit={e=>{e.preventDefault();adjust()}}><div style={{fontSize:12,color:'#777'}}>Variant: <b>{selected.name||selected.sku||selected.id}</b></div><label>Quantity change<input type="number" required value={qty} onChange={e=>setQty(e.target.value)} placeholder="e.g. 10 or -2"/></label><label>Reason<textarea required value={reason} onChange={e=>setReason(e.target.value)} placeholder="Stock receipt, damaged item, cycle count…"/></label><div className="form-actions"><Button type="button" onClick={()=>setSelected(null)}>Cancel</Button><Button className="primary" disabled={busy}>{busy?'Saving…':'Apply adjustment'}</Button></div></form></Modal>}</section>}
