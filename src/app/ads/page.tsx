'use client';import{useEffect,useState}from'react';import{api}from'@/lib/api';import{API_ROUTES}from'@/lib/api-contract';import{Card,PageHeader,Button,Badge,Loading,ErrorState}from'@/components/ui';export default function Ads(){const[d,setD]=useState<any>(null),[e,setE]=useState('');async function load(){try{const r=await api<any>(API_ROUTES.ads.status);setD(r.data)}catch(x){setE(x instanceof Error?x.message:'Unable to load ads status')}}useEffect(()=>{load()},[]);return <section className="content"><PageHeader title="Ads Control" description="Meta advertising assets, audiences, campaigns and insights." action={<Button onClick={load}>Refresh</Button>}/>{e&&<ErrorState error={e} onRetry={load}/>} {!d&&!e?<Loading/>:<div className="section-grid"><Card><div className="card-title">Meta connection</div><div className="quick"><div><b>Status</b><span><Badge>{d?.connected?'Connected':'Not connected'}</Badge></span></div><div><b>Ad account</b><span>{d?.ad_account_name||d?.ad_account_id||'Not selected'}</span></div></div></Card><Card><div className="card-title">Advertising controls</div><p className="muted">Provider credentials remain server-side. Campaign, ad set, creative, audience and insight APIs are exposed through PriyasaCore.</p></Card></div>}</section>}