'use client';
import {useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import {AlertTriangle,CheckCircle2,Clock3,PackageCheck,RefreshCw,RotateCcw,WalletCards} from 'lucide-react';
import {api} from '@/lib/api';
import {Card,PageHeader,DataToolbar,Button,Modal,Loading,ErrorState,Badge} from '@/components/ui';
import {money,dateTime,titleCase,apiMessage} from '@/lib/format';

type ReturnRow={id:number|string;status?:string;reason?:string|null;note?:string|null;amount?:number|string|null;refund_amount?:number|string|null;created_at?:string;updated_at?:string;order?:{id?:number|string;order_number?:string;grand_total?:number|string;total?:number|string;currency?:string}|null;order_id?:number|string;order_number?:string;customer?:{name?:string;phone?:string;email?:string}|null};
const statuses=['','requested','approved','rejected','received','refunded'];
const terminal=new Set(['rejected','refunded']);
const amountOf=(r:ReturnRow)=>r.refund_amount??r.amount??r.order?.grand_total??r.order?.total??null;
const moneyIn=(r:ReturnRow)=>amountOf(r)==null?'—':money(Number(amountOf(r)),r.order?.currency||'INR');
export default function Returns(){
 const [rows,setRows]=useState<ReturnRow[]>([]),[q,setQ]=useState(''),[statusFilter,setStatusFilter]=useState(''),[selected,setSelected]=useState<ReturnRow|null>(null),[status,setStatus]=useState(''),[note,setNote]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false);
 async function load(){setError('');setLoading(true);try{const r=await api<ReturnRow[]|{data:ReturnRow[]}>('/admin/returns');const d=r.data;setRows(Array.isArray(d)?d:(d?.data||[]))}catch(e){setError(apiMessage(e,'Unable to load returns'))}finally{setLoading(false)}}
 useEffect(()=>{load()},[]);
 const filtered=useMemo(()=>rows.filter(v=>{if(statusFilter&&v.status!==statusFilter)return false;return JSON.stringify(v).toLowerCase().includes(q.trim().toLowerCase())}),[rows,q,statusFilter]);
 const metrics=useMemo(()=>({requested:rows.filter(r=>r.status==='requested').length,approved:rows.filter(r=>r.status==='approved').length,received:rows.filter(r=>r.status==='received').length,refunded:rows.filter(r=>r.status==='refunded').length,value:rows.filter(r=>r.status==='refunded').reduce((s,r)=>s+Number(amountOf(r)||0),0)}),[rows]);
 function review(r:ReturnRow){setSelected(r);setStatus(r.status||'requested');setNote(r.note||'');setError('')}
 async function update(){if(!selected||!status||(status===selected.status&&!note.trim())||terminal.has(selected.status||''))return;if(status==='refunded'&&selected.status!=='refunded'&&!window.confirm('Mark this return as refunded? Confirm the financial refund has actually been processed before changing this state.'))return;if(status==='rejected'&&!window.confirm('Reject this return request? This decision will be recorded by Core.'))return;setBusy(true);setError('');try{await api(`/admin/returns/${selected.id}/status`,{method:'POST',body:JSON.stringify({status,note:note.trim()||undefined})});setSelected(null);setNote('');await load()}catch(e){setError(apiMessage(e,'Return update failed'))}finally{setBusy(false)}}
 const cards=[['Requested',metrics.requested,Clock3],['Approved',metrics.approved,PackageCheck],['Received',metrics.received,RotateCcw],['Refunded',metrics.refunded,CheckCircle2]] as const;
 return <section className="content"><PageHeader title="Returns & Reverse Logistics" description="Operate approval, receipt and refund queues while keeping Core authoritative for return state." action={<Button onClick={load} disabled={loading||busy}><RefreshCw size={14}/> {loading?'Loading…':'Refresh'}</Button>}/>{error&&!selected&&<ErrorState error={error} onRetry={load}/>}<div className="grid">{cards.map(([label,value,Icon])=><Card key={label}><div className="metric-label">{label}</div><div className="metric">{value}</div><Icon size={18}/></Card>)}</div><div className="section-grid" style={{marginTop:16}}><Card><div className="card-title">Refunded value</div><div className="metric">{money(metrics.value)}</div><div className="metric-label">From loaded return records</div></Card><Card><div className="card-title">Workflow</div><div className="quick" style={{marginTop:12}}><div><b>Requested → Approved</b><span>Review eligibility and evidence.</span></div><div><b>Approved → Received</b><span>Confirm the returned parcel has arrived.</span></div><div><b>Received → Refunded</b><span>Only record refunded after the financial operation is complete.</span></div></div></Card></div><Card style={{marginTop:16}}><DataToolbar value={q} onChange={setQ} placeholder="Search return, order, customer…"><select className="filter-select" value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}>{statuses.map(s=><option key={s} value={s}>{s?titleCase(s):'All statuses'}</option>)}</select><Button onClick={load} disabled={loading}>Refresh</Button></DataToolbar>{loading?<Loading/>:filtered.length?<div className="table-wrap"><table className="table"><thead><tr><th>Return</th><th>Order</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th><th/></tr></thead><tbody>{filtered.map((r,i)=><tr key={String(r.id||i)}><td><b>#{r.id||'—'}</b></td><td>{r.order?.order_number||r.order_number||r.order_id||'—'}</td><td>{r.customer?.name||r.customer?.phone||'—'}</td><td>{moneyIn(r)}</td><td><Badge>{titleCase(r.status||'unknown')}</Badge></td><td>{r.created_at?dateTime(r.created_at):'—'}</td><td>{r.id&&<Button onClick={()=>review(r)}>Review</Button>}</td></tr>)}</tbody></table></div>:<div className="empty"><strong>No returns</strong><span>No return requests match the current filters.</span></div>}</Card>{selected&&<Modal title={`Return #${selected.id}`} onClose={()=>setSelected(null)}><div className="form"><div className="form-grid"><Card><div className="metric-label">Order</div><b><Link href={`/orders/${selected.order?.id||selected.order_id||'#'}`}>#{selected.order?.order_number||selected.order_number||selected.order_id||'—'}</Link></b><div className="muted">{selected.customer?.name||'Customer'} · {selected.customer?.phone||selected.customer?.email||'—'}</div></Card><Card><div className="metric-label">Refund value</div><b>{moneyIn(selected)}</b><div className="muted">Current: {titleCase(selected.status||'unknown')}</div></Card></div><div className="card"><div className="metric-label">Reason</div><div style={{fontSize:13,marginTop:6}}>{selected.reason||'No reason supplied.'}</div></div><label>Status<select value={status} disabled={busy||terminal.has(selected.status||'')} onChange={e=>setStatus(e.target.value)}>{statuses.filter(Boolean).map(s=><option key={s} value={s}>{titleCase(s)}</option>)}</select></label><label>Internal note<textarea value={note} disabled={busy} onChange={e=>setNote(e.target.value)} placeholder="QC / approval / refund processing note…"/></label>{terminal.has(selected.status||'')&&<div className="muted"><AlertTriangle size={14}/> This return is terminal and cannot be changed from Admin.</div>}{error&&<div className="form-error">{error}</div>}<div className="form-actions"><Button onClick={()=>setSelected(null)} disabled={busy}>Close</Button><Button className="primary" disabled={busy||terminal.has(selected.status||'')||(status===selected.status&&!note.trim())} onClick={update}>{busy?'Saving…':'Save return state'}</Button></div></div></Modal>}</section>}
